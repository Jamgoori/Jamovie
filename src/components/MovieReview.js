import { useSelect } from '@mui/base'
import React from 'react'

const MovieReview = ({}) => {
    const{MovieReview} = useSelect(state=>state.detail)
  return (
    <>
    
    </>
  )
}

export default MovieReview